# InkPanel admin UI redesign constraint

This feature changes only the web/admin experience.

## Hard constraint

**Do not change the e-ink renderer.**

The existing 800×480 monochrome panel design, render templates/CSS, framebuffer conversion, content hashing, source rendering, panel protocol and firmware behaviour are out of scope.

The redesign may change only the browser-facing administration UI and its presentation/navigation.
