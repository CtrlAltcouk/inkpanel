[Unit]
Description=Update inkpanel from git
# Separate unit, so restarting inkpanel.service does not kill this mid-flight.

[Service]
Type=oneshot
WorkingDirectory=/opt/inkpanel/app
ExecStart=/usr/local/bin/inkpanel-update
